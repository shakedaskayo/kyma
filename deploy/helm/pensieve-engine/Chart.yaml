apiVersion: v2
name: pensieve-engine
description: The pensieve context engine (web UI + API + ingest) on Kubernetes.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Default engine image tag; `pensieve deploy` overrides image.tag to match the CLI.
appVersion: "latest"
home: https://github.com/shakedaskayo/pensieve
sources:
  - https://github.com/shakedaskayo/pensieve
