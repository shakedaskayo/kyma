apiVersion: v2
name: kyma-engine
description: The kyma context engine (web UI + API + ingest) on Kubernetes.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Default engine image tag; `kyma deploy` overrides image.tag to match the CLI.
appVersion: "latest"
home: https://github.com/shakedaskayo/kyma
sources:
  - https://github.com/shakedaskayo/kyma
