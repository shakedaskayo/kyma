apiVersion: v1
kind: Service
metadata:
  name: {{ include "kyma-engine.fullname" . }}
  labels:
    {{- include "kyma-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "kyma-engine.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
