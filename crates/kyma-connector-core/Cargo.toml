[package]
name = "kyma-connector-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Stable extension API for third-party kyma connectors — the Connector trait, ConnectorCtx/Run, CatalogEntry, and the Credential interfaces. Customers depend on this crate (not kyma-connectors) so adding a new connector requires no core-repo changes."

[lints]
workspace = true

[dependencies]
kyma-connectors.workspace = true
kyma-core.workspace = true
