[package]
name = "kyma-datasource-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Stable extension API for third-party kyma data sources — the DataSource trait, DataSourceCtx/Run, CatalogEntry, and the Credential interfaces. Customers depend on this crate (not kyma-datasources) so adding a new data source requires no core-repo changes."

[lints]
workspace = true

[dependencies]
kyma-datasources.workspace = true
kyma-core.workspace = true
