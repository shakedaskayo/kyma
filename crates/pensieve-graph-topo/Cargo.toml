[package]
name = "pensieve-graph-topo"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Immutable CSR graph topology + multi-source BFS traversal (k-hop / var-length / shortest-path), the substrate for >2-hop graph queries (S3.1)."

[lints]
workspace = true

[dependencies]

[dev-dependencies]
# Differential correctness is validated against the petgraph reference oracle.
# The testkit is publish=false and brings petgraph in only as a TEST-time
# transitive dep, so petgraph never enters this engine crate's release build.
pensieve-graph-testkit.workspace = true
proptest.workspace = true
