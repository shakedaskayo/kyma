[workspace]
resolver = "2"
members = [
    "crates/pensieve-core",
    "crates/pensieve-format-tlm",
    "crates/pensieve-format-parquet",
    "crates/pensieve-format-stub-vec",
    "crates/pensieve-catalog",
    "crates/pensieve-catalog-sqlite",
    "crates/pensieve-storage",
    "crates/pensieve-ingest-core",
    "crates/pensieve-ingest-rest",
    "crates/pensieve-ingest-filedrop",
    "crates/pensieve-ingest-otlp",
    "crates/pensieve-ingest-kafka",
    "crates/pensieve-compaction",
    "crates/pensieve-datasources",
    "crates/pensieve-datasource-core",
    "crates/pensieve-codeparse",
    "crates/pensieve-jobs",
    "crates/pensieve-index-vector",
    "crates/pensieve-index-fts",
    "crates/pensieve-kql",
    "crates/pensieve-graph",
    "crates/pensieve-plan",
    "crates/pensieve-exec",
    "crates/pensieve-federation",
    "crates/pensieve-server",
    "crates/pensieve-cli",
    "crates/pensieve-bin",
    "crates/pensieve-local",
    "crates/pensieve-web-assets",
    "crates/pensieve-embed",
    "crates/pensieve-memory",
    "crates/pensieve-artifact-graph",
    "crates/pensieve-mcp",
    "crates/pensieve-queue",
    "crates/pensieve-redact",
    "crates/pensieve-ccmem",
    "crates/pensieve-brain",
    "crates/pensieve-retrieval-eval",
    "crates/pensieve-graph-testkit",
    "crates/pensieve-graph-bench",
    "crates/pensieve-graph-topo",
    "web/src-tauri",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.95"
license = "MIT"
repository = "https://github.com/shakedaskayo/pensieve"
authors = ["pensieve contributors"]

[workspace.lints.rust]
unsafe_op_in_unsafe_fn = "deny"
# missing_docs tightens back to "warn" in M6 hardening (see plan §9).
# During scaffolding + M1-M5, crate/module-level docs are required but
# per-field docs are noise.
missing_docs = "allow"
unreachable_pub = "warn"

[workspace.lints.clippy]
pedantic = { level = "warn", priority = -1 }
must_use_candidate = "allow"
module_name_repetitions = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"
uninlined_format_args = "allow"

[workspace.dependencies]
# Core async + error plumbing
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
anyhow = "1"
thiserror = "2"

# Tracing / observability
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tracing-opentelemetry = "0.28"
opentelemetry = "0.27"
opentelemetry_sdk = { version = "0.27", features = ["rt-tokio"] }

# Arrow + DataFusion stack — keep these in lockstep; DF 44 depends on arrow 53.x
arrow = "53"
arrow-array = "53"
arrow-schema = "53"
arrow-json = "53"
arrow-flight = "53"
parquet = { version = "53", features = ["arrow", "async", "zstd"] }
datafusion = "44"
# Query federation (remote subplan pushdown). Pinned: 0.3.5 is the last release
# on the DataFusion 44 line (0.3.6+ moved to DF 45/46; 0.4/0.5 need newer still).
# Bump alongside any future DF upgrade.
datafusion-federation = { version = "=0.3.5", features = ["sql"] }
# TDS (SQL Server wire protocol) client — Microsoft Fabric SQL endpoints.
tiberius = { version = "0.12", default-features = false, features = ["tds73", "rustls", "chrono"] }
tokio-util = { version = "0.7", features = ["compat"] }

# Storage
object_store = { version = "0.11", features = ["aws"] }

# Catalog
sqlx = { version = "0.8", features = ["runtime-tokio-rustls", "postgres", "uuid", "chrono", "json", "macros"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ciborium = "0.2"           # CBOR for `dynamic` column
prost = "0.13"
tonic = "0.12"
tonic-web = "0.12"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "http2", "stream"] }

# Networking
axum = { version = "0.7", features = ["macros", "http2", "ws"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["trace", "cors", "compression-gzip", "request-id", "util"] }
hyper = "1"
http-body-util = "0.1"
simd-json = "0.14"

# Parser
chumsky = "0.10"

# Secret detection / redaction
regex = "1"
aho-corasick = "1"

# Indices + compression
fst = "0.4"
zstd = "0.13"
lz4_flex = "0.11"
bytes = "1"

# Ingest sources
rdkafka = { version = "0.37", features = ["cmake-build", "tokio", "tracing"] }
opentelemetry-proto = { version = "0.27", features = ["with-serde", "gen-tonic-messages"] }

# IDs, time, misc
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
futures = "0.3"

# Test infra
proptest = "1"
testcontainers = "0.23"
testcontainers-modules = { version = "0.11", features = ["postgres", "minio"] }
criterion = { version = "0.5", features = ["async_tokio", "html_reports"] }

# Metrics (Prometheus via the `metrics` facade crate)
metrics = "0.24"
metrics-exporter-prometheus = { version = "0.17", default-features = false }
fastrand = "2"

# Inter-crate dependencies (always use workspace = true). Each carries a
# `version` alongside `path` so the closure is publishable to crates.io: cargo
# uses the path for workspace builds and the version requirement for published
# crates. Kept in lockstep with [workspace.package] version.
pensieve-core              = { path = "crates/pensieve-core",            version = "0.1.0" }
pensieve-format-tlm        = { path = "crates/pensieve-format-tlm",      version = "0.1.0" }
pensieve-format-parquet    = { path = "crates/pensieve-format-parquet",  version = "0.1.0" }
pensieve-format-stub-vec   = { path = "crates/pensieve-format-stub-vec", version = "0.1.0" }
pensieve-graph-testkit     = { path = "crates/pensieve-graph-testkit",   version = "0.1.0" }
pensieve-graph-topo        = { path = "crates/pensieve-graph-topo",      version = "0.1.0" }
pensieve-catalog           = { path = "crates/pensieve-catalog",         version = "0.1.0" }
pensieve-catalog-sqlite    = { path = "crates/pensieve-catalog-sqlite",  version = "0.1.0" }
pensieve-local             = { path = "crates/pensieve-local",           version = "0.1.0" }
pensieve-storage           = { path = "crates/pensieve-storage",         version = "0.1.0" }
pensieve-ingest-core       = { path = "crates/pensieve-ingest-core",     version = "0.1.0" }
pensieve-ingest-rest       = { path = "crates/pensieve-ingest-rest",     version = "0.1.0" }
pensieve-ingest-filedrop   = { path = "crates/pensieve-ingest-filedrop", version = "0.1.0" }
pensieve-ingest-otlp       = { path = "crates/pensieve-ingest-otlp",     version = "0.1.0" }
pensieve-ingest-kafka      = { path = "crates/pensieve-ingest-kafka",    version = "0.1.0" }
pensieve-graph             = { path = "crates/pensieve-graph",           version = "0.1.0" }
pensieve-kql               = { path = "crates/pensieve-kql",             version = "0.1.0" }
pensieve-compaction        = { path = "crates/pensieve-compaction",      version = "0.1.0" }
pensieve-datasources        = { path = "crates/pensieve-datasources",      version = "0.1.0" }
pensieve-datasource-core    = { path = "crates/pensieve-datasource-core",  version = "0.1.0" }
pensieve-codeparse         = { path = "crates/pensieve-codeparse",       version = "0.1.0" }
pensieve-jobs              = { path = "crates/pensieve-jobs",            version = "0.1.0" }
pensieve-index-vector      = { path = "crates/pensieve-index-vector",    version = "0.1.0" }
pensieve-index-fts         = { path = "crates/pensieve-index-fts",        version = "0.1.0" }
pensieve-plan              = { path = "crates/pensieve-plan",            version = "0.1.0" }
pensieve-exec              = { path = "crates/pensieve-exec",            version = "0.1.0" }
pensieve-federation        = { path = "crates/pensieve-federation",      version = "0.1.0" }
pensieve-server            = { path = "crates/pensieve-server",          version = "0.1.0" }
pensieve-cli               = { path = "crates/pensieve-cli",             version = "0.1.0" }
pensieve-web-assets        = { path = "crates/pensieve-web-assets",      version = "0.1.0" }
pensieve-embed             = { path = "crates/pensieve-embed",           version = "0.1.0" }
pensieve-memory            = { path = "crates/pensieve-memory",          version = "0.1.0" }
pensieve-artifact-graph    = { path = "crates/pensieve-artifact-graph",  version = "0.1.0" }
pensieve-mcp               = { path = "crates/pensieve-mcp",             version = "0.1.0" }
pensieve-queue             = { path = "crates/pensieve-queue",           version = "0.1.0" }
pensieve-redact            = { path = "crates/pensieve-redact",          version = "0.1.0" }
pensieve-ccmem             = { path = "crates/pensieve-ccmem",           version = "0.1.0" }
pensieve-brain             = { path = "crates/pensieve-brain",           version = "0.1.0" }

# Auth
jsonwebtoken = "9"

# Embeddings + vector search
fastembed = "4"
pgvector = { version = "0.4", features = ["sqlx", "serde"] }
ndarray = "0.16"
schemars = "0.8"

# Agent stack (ADK-Rust) — feature set trimmed to only what the inline
# `/v1/agent/ask` endpoint needs. See crates/pensieve-server/src/agent/.
adk-rust = { version = "0.6", default-features = false, features = [
    "agents",
    "models",
    "ollama",
    "anthropic",
    "openai",
    "tools",
    "sessions",
    "runner",
] }

# Vendored arrow-arith patch: upstream 53.4.1 calls `d.quarter()` via the
# `ChronoDateExt` extension trait, which collides with the `Datelike::quarter`
# default method chrono added in 0.4.44. adk-core requires chrono >= 0.4.44,
# and DataFusion 44 is locked to the arrow 53.x line, so neither end can move.
# The patch is a one-line disambiguation (explicit `Datelike::quarter`) plus a
# loosened chrono upper bound in arrow-arith's own manifest. Revisit when
# DataFusion ships a release that tracks an arrow-arith line compatible with
# chrono >= 0.4.44 natively.
[patch.crates-io]
arrow-arith = { path = "third_party/arrow-arith-53-patch" }

[profile.release]
lto = "thin"
codegen-units = 1
debug = "line-tables-only"
strip = "debuginfo"

[profile.bench]
lto = "thin"
codegen-units = 1
debug = "line-tables-only"

[profile.dev]
opt-level = 1
debug = true
