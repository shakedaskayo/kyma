name: backcompat

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  replay:
    name: replay fixed query set against every fixture
    runs-on: ubuntu-22.04
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: pensieve
          POSTGRES_PASSWORD: pensieve_dev
          POSTGRES_DB: pensieve
        ports: ["5432:5432"]
        options: >-
          --health-cmd="pg_isready -U pensieve"
          --health-interval=5s
          --health-timeout=3s
          --health-retries=10
      minio:
        image: bitnamilegacy/minio:2025.7.23-debian-12-r5
        env:
          MINIO_ROOT_USER: pensieve_admin
          MINIO_ROOT_PASSWORD: pensieve_admin_dev
          MINIO_DEFAULT_BUCKETS: pensieve
        ports: ["9000:9000"]
    env:
      # Env var names match what crates/pensieve-bin/src/main.rs + crates/pensieve-storage/src/lib.rs
      # actually read. Verified against source: PENSIEVE_CATALOG_URL, PENSIEVE_S3_ACCESS_KEY_ID,
      # PENSIEVE_S3_SECRET_ACCESS_KEY (not the shorter forms used in some docs).
      PENSIEVE_CATALOG_URL: postgresql://pensieve:pensieve_dev@localhost:5432/pensieve
      PENSIEVE_S3_ENDPOINT: http://localhost:9000
      PENSIEVE_S3_BUCKET: pensieve
      PENSIEVE_SECRET_KEY: pensieve-ci-secret-not-for-production
      PENSIEVE_S3_REGION: us-east-1
      PENSIEVE_S3_ACCESS_KEY_ID: pensieve_admin
      PENSIEVE_S3_SECRET_ACCESS_KEY: pensieve_admin_dev
      PENSIEVE_S3_PATH_STYLE: "true"
      PENSIEVE_S3_ALLOW_HTTP: "true"
      PENSIEVE_GRPC_ADDR: "off"
      PENSIEVE_OTLP_ADDR: "off"
      # AWS SDK fallback credentials (some object-store libs read these).
      AWS_ACCESS_KEY_ID: pensieve_admin
      AWS_SECRET_ACCESS_KEY: pensieve_admin_dev
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: backcompat-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}

      - name: install postgresql client (for pg_dump used by scripts)
        run: sudo apt-get update && sudo apt-get install -y postgresql-client cmake pkg-config libssl-dev libsasl2-dev libcurl4-openssl-dev zlib1g-dev libzstd-dev protobuf-compiler

      - name: build engine
        run: cargo build --release -p pensieve-bin

      - name: start engine
        run: |
          ./target/release/pensieve > /tmp/pensieve-engine.log 2>&1 &
          echo $! > /tmp/engine.pid
          for i in $(seq 1 60); do
            if curl -fsS http://localhost:8080/health >/dev/null; then break; fi
            sleep 1
          done
          curl -fsS http://localhost:8080/health || { tail -50 /tmp/pensieve-engine.log; exit 1; }

      - name: seed engine with each fixture's seed dataset
        run: |
          for fixture in scripts/fixtures/backcompat/*/; do
            seed="$fixture/seed.ndjson"
            if [ -f "$seed" ]; then
              echo "seeding from $seed"
              curl -fsS -X POST http://localhost:8080/v1/ingest \
                -H "X-Database: obs" \
                -H "X-Table: otel_logs" \
                -H "Content-Type: application/x-ndjson" \
                --data-binary "@$seed"
            fi
          done
          # allow commit coordinator to flush
          sleep 3
          # warm Prometheus metric labels by running each query class once
          # (per F1.16 finding: metric label set initializes on first use)
          curl -sS -X POST http://localhost:8080/v1/query -H "X-Database: obs" -H "Content-Type: application/x-kql" --data-binary 'otel_logs | summarize n = count()' >/dev/null
          curl -sS -X POST http://localhost:8080/v1/query -H "X-Database: obs" -H "Content-Type: application/sql" --data-binary 'SELECT 1' >/dev/null

      - name: replay each fixture
        run: |
          fail=0
          for fixture in scripts/fixtures/backcompat/*/; do
            echo "::group::$(basename "$fixture")"
            if ! scripts/backcompat-replay.sh http://localhost:8080 "$fixture" scripts/backcompat-queries.txt; then
              fail=1
            fi
            echo "::endgroup::"
          done
          exit $fail

      - name: dump engine logs on failure
        if: failure()
        run: |
          echo "=== engine stdout/stderr (tail 200) ==="
          tail -200 /tmp/pensieve-engine.log

      - name: stop engine
        if: always()
        run: kill "$(cat /tmp/engine.pid)" 2>/dev/null || true
